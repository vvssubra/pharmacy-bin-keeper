---
phase: 01-english-ui
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/components/AppSidebar.test.tsx
  - src/components/DrugFormDialog.test.tsx
  - src/pages/Index.test.tsx
  - src/pages/DoctorRequest.test.tsx
autonomous: true
requirements:
  - ENGL-01
  - ENGL-02
  - ENGL-03
  - ENGL-04

must_haves:
  truths:
    - "Four test files exist and every test in them FAILS (RED) because the source files still contain Malay strings"
    - "Each test file imports the real component under test and asserts on English strings that do not yet exist"
    - "Running npx vitest run exits non-zero until the translation work in Plans 02-05 makes them pass"
  artifacts:
    - path: "src/components/AppSidebar.test.tsx"
      provides: "Failing tests for ENGL-01 (nav labels in English)"
      contains: "New Requests"
    - path: "src/components/DrugFormDialog.test.tsx"
      provides: "Failing tests for ENGL-02 (form labels) and ENGL-04 (Zod messages)"
      contains: "Drug name is required"
    - path: "src/pages/Index.test.tsx"
      provides: "Failing tests for ENGL-03 (status badge values)"
      contains: "CRITICAL"
    - path: "src/pages/DoctorRequest.test.tsx"
      provides: "Failing tests for ENGL-04 (Zod validation messages)"
      contains: "Patient name is required"
  key_links:
    - from: "src/components/AppSidebar.test.tsx"
      to: "src/components/AppSidebar.tsx"
      via: "import and render"
      pattern: "import.*AppSidebar"
---

<objective>
Create four failing test files (Wave 0 scaffolds) that will become the RED phase of TDD. These tests import the real components and assert that English strings are present. They MUST fail now because the source files still contain Malay strings. Plans 02-05 will make them pass.

Purpose: Nyquist compliance — every translation task has an automated verify command. No translation work happens without a test watching it.
Output: Four test files, all failing, covering ENGL-01 through ENGL-04.
</objective>

<execution_context>
@/Users/subramaniampalaniapan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/subramaniampalaniapan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-english-ui/01-RESEARCH.md
@.planning/phases/01-english-ui/01-VALIDATION.md
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Write failing tests for AppSidebar (ENGL-01) and DrugFormDialog (ENGL-02 + ENGL-04)</name>
  <files>src/components/AppSidebar.test.tsx, src/components/DrugFormDialog.test.tsx</files>
  <read_first>
    - src/components/AppSidebar.tsx (read in full — understand current render structure and props)
    - src/components/DrugFormDialog.tsx (read in full — understand form fields, Zod schema, button text)
    - src/components/ProtectedRoute.test.tsx (read to understand test patterns already used in this project)
  </read_first>
  <behavior>
    AppSidebar.test.tsx assertions (all will FAIL initially because the source has Malay):
    - Renders sidebar with screen.getByText("New Requests") — currently "Permintaan Baharu"
    - Renders sidebar with screen.getByText("Patients") — currently "Pesakit"
    - Renders sidebar with screen.getByText("Reports") — currently "Laporan"
    - Renders sidebar with screen.getByText("Role Management") — currently "Pengurusan Peranan"

    DrugFormDialog.test.tsx assertions (all will FAIL initially):
    - Add Drug dialog title renders "Add Drug" — currently "Tambah Ubat"
    - Form label renders "Drug Name *" — currently "Nama Ubat *"
    - Form label renders "Unit of Measure" — currently "Unit Pengukuran"
    - Submit button renders "Save" — currently "Simpan"
    - Zod validation: submitting empty form shows "Drug name is required" — currently "Nama ubat diperlukan"
  </behavior>
  <action>
    Create src/components/AppSidebar.test.tsx:
    - Import render, screen from @testing-library/react
    - Import AppSidebar from ./AppSidebar
    - Mock Supabase auth and user_roles query so the sidebar renders without network (copy pattern from ProtectedRoute.test.tsx)
    - Mock react-router-dom useLocation and useNavigate (sidebar uses navigation)
    - Write describe("AppSidebar navigation labels") with test cases listed in behavior section
    - Each test: render(<AppSidebar />) and assert getByText(englishLabel) — these will throw until translation

    Create src/components/DrugFormDialog.test.tsx:
    - Import render, screen, fireEvent, waitFor from @testing-library/react
    - Import DrugFormDialog from ./DrugFormDialog
    - Mock Supabase query for drugs list (the dialog does a drugs fetch)
    - Write describe("DrugFormDialog English labels") with test cases for Add Drug mode (pass isOpen=true, mode="add", drug=null props)
    - Write describe("DrugFormDialog Zod validation") with test case: render form, click Save without input, await validation message
    - Use screen.getByText / getByRole / getByLabelText for assertions

    IMPORTANT: The tests must import the REAL components (not mocks). Mocking is only for external dependencies (Supabase, router). The component code itself must be exercised.
  </action>
  <verify>
    <automated>npx vitest run src/components/AppSidebar.test.tsx src/components/DrugFormDialog.test.tsx 2>&1 | tail -20</automated>
  </verify>
  <done>Both test files exist and compile without TypeScript errors. Tests FAIL (non-zero exit) because English strings are not yet in source. Failure messages show "Unable to find an element with the text: New Requests" (or equivalent) — not import/compile errors.</done>
</task>

<task type="tdd">
  <name>Task 2: Write failing tests for Index (ENGL-03) and DoctorRequest (ENGL-04 Zod)</name>
  <files>src/pages/Index.test.tsx, src/pages/DoctorRequest.test.tsx</files>
  <read_first>
    - src/pages/Index.tsx (read in full — understand status badge rendering, stat card labels, column headers)
    - src/pages/DoctorRequest.tsx (read in full — understand Zod schema messages and form labels)
    - src/components/ProtectedRoute.test.tsx (read to understand mock patterns)
  </read_first>
  <behavior>
    Index.test.tsx assertions (all will FAIL initially):
    - Status badge "CRITICAL" renders (currently "KRITIKAL")
    - Status badge "LOW" renders (currently "RENDAH")
    - Status badge "EXCESS" renders (currently "LEBIHAN")
    - Column header "Drug Name" renders (currently "Nama Ubat")
    - Column header "Actions" renders (currently "Tindakan")

    DoctorRequest.test.tsx assertions (all will FAIL initially):
    - Zod validation: submitting without patient name shows "Patient name is required" (currently "Nama pesakit diperlukan")
    - Zod validation: submitting without drug shows "Please select a drug" (currently "Sila pilih ubat")
    - Form label "Patient Name *" renders (currently "Nama Pesakit *")
    - Form label "Drug *" renders (currently "Ubat *")
    - Submit button "Submit Request" renders (currently... varies)
  </behavior>
  <action>
    Create src/pages/Index.test.tsx:
    - Import render, screen from @testing-library/react
    - Import Index from ./Index
    - Mock Supabase: mock useQuery hooks that fetch drugs and transactions — return a stub dataset containing one drug with stok_status="kritikal" so the KRITIKAL badge renders
    - Stub data: { drug_name: "Drug A", stok_status: "kritikal", current_stock: 0 }
    - Mock react-router-dom useNavigate
    - Write describe("Index dashboard English text") with test cases for status badges and column headers
    - Use screen.getAllByText / getByText for assertions

    Create src/pages/DoctorRequest.test.tsx:
    - Import render, screen, fireEvent, waitFor from @testing-library/react
    - Import DoctorRequest from ./DoctorRequest
    - Mock Supabase drugs query to return empty array (we only need to test form labels and validation)
    - Mock useAuth to return a logged-in doctor user
    - Mock react-router-dom
    - Write describe("DoctorRequest form labels") — assert English labels render
    - Write describe("DoctorRequest Zod validation") — click submit button without filling required fields, await error messages in English
    - For Zod trigger: render form, find submit button by text, fireEvent.click, await screen.findByText("Patient name is required")

    IMPORTANT: Tests must FAIL now (RED). The goal is the test file exists, compiles, and fails with "Unable to find element" errors rather than import errors.
  </action>
  <verify>
    <automated>npx vitest run src/pages/Index.test.tsx src/pages/DoctorRequest.test.tsx 2>&1 | tail -20</automated>
  </verify>
  <done>Both test files exist and compile without TypeScript errors. Tests FAIL with "Unable to find an element" type errors (not TypeScript compile errors). Running npx vitest run from project root shows all 4 new test files failing.</done>
</task>

</tasks>

<verification>
After both tasks: run `npx vitest run` from project root. The 4 new test files must appear in the output and all must FAIL. Zero TypeScript compile errors.

Run: `ls src/components/AppSidebar.test.tsx src/components/DrugFormDialog.test.tsx src/pages/Index.test.tsx src/pages/DoctorRequest.test.tsx` — all 4 files must exist.
</verification>

<success_criteria>
- Four test files exist at the correct paths
- All four files compile without TypeScript errors
- All test assertions fail with "element not found" errors (not import/compile errors)
- npx vitest run exits non-zero
- No watch-mode flags used
</success_criteria>

<output>
After completion, create `.planning/phases/01-english-ui/01-01-SUMMARY.md` documenting:
- The 4 test files created and what each covers
- The exact failure messages seen (so later plans know tests are RED, not broken)
- Any mock patterns established for downstream test files to reuse
</output>
