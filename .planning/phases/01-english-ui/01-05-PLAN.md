---
phase: 01-english-ui
plan: 05
type: execute
wave: 4
depends_on:
  - "01-03"
  - "01-04"
files_modified:
  - src/pages/SpecialistDashboard.tsx
  - src/pages/PatientRegistry.tsx
  - src/pages/DoctorRequest.tsx
  - src/pages/AntibioticForm.tsx
  - src/pages/RoleManagement.tsx
autonomous: true
requirements:
  - ENGL-02
  - ENGL-03
  - ENGL-04
  - ENGL-05

must_haves:
  truths:
    - "SpecialistDashboard shows English stat labels, tab labels, dialogs, and toasts"
    - "PatientRegistry shows English page title, table headers, search UI, and toasts"
    - "DoctorRequest shows English form labels, Zod messages, status badges, and success screen"
    - "AntibioticForm and RoleManagement contain no Malay user-visible strings"
    - "date-fns ms locale removed from SpecialistDashboard and PatientRegistry"
  artifacts:
    - path: "src/pages/DoctorRequest.tsx"
      provides: "English form UI + Zod messages"
      contains: "Patient name is required"
    - path: "src/pages/SpecialistDashboard.tsx"
      provides: "English specialist UI"
      contains: "Approved Today"
    - path: "src/pages/PatientRegistry.tsx"
      provides: "English patient registry UI"
      contains: "Last Visit"
    - path: "src/pages/RoleManagement.tsx"
      provides: "English role management UI"
      contains: "Role updated successfully."
  key_links:
    - from: "src/pages/DoctorRequest.tsx"
      to: "src/pages/DoctorRequest.test.tsx"
      via: "test imports component"
      pattern: "Patient name is required"
---

<objective>
Translate the remaining 5 pages: specialist dashboard, patient registry, doctor request form, antibiotic form, and role management. After this plan, DoctorRequest.test.tsx from Plan 01 must turn GREEN. This completes the translation across all 19 files.

Purpose: Final translation pass — all remaining pages go to English.
Output: 5 pages fully in English; DoctorRequest.test.tsx passes; all 4 scaffold tests from Plan 01 are GREEN.
</objective>

<execution_context>
@/Users/subramaniampalaniapan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/subramaniampalaniapan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-english-ui/01-RESEARCH.md
@.planning/phases/01-english-ui/01-03-SUMMARY.md
@.planning/phases/01-english-ui/01-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Translate SpecialistDashboard.tsx and PatientRegistry.tsx</name>
  <files>src/pages/SpecialistDashboard.tsx, src/pages/PatientRegistry.tsx</files>
  <read_first>
    - src/pages/SpecialistDashboard.tsx (read in full — stat card labels, tab labels, data labels, buttons, placeholders, toasts, date-fns locale — 4 formatDistanceToNow calls)
    - src/pages/PatientRegistry.tsx (read in full — page title, search placeholder, table headers, dialog labels, toasts, date-fns locale)
  </read_first>
  <action>
    SpecialistDashboard.tsx replacements:

    date-fns locale (CRITICAL):
    - Remove import { ms } from "date-fns/locale"
    - Remove { locale: ms } from ALL 4 formatDistanceToNow() calls

    Stat card labels:
    - "Menunggu (Ubat)" → "Pending (Drug)"
    - "Menunggu (Antibiotik)" → "Pending (Antibiotic)"
    - "Diluluskan Hari Ini" → "Approved Today"
    - "Ditolak Hari Ini" → "Rejected Today"

    Tab labels:
    - "Ubat Kawalan" → "Controlled Drug"
    - Any other tab labels in Malay → translate

    Data labels (read the file to find all — the research says "audit all inline text"):
    - Translate all Malay field labels in request cards (patient labels, drug labels, etc.)
    - "Batal" → "Cancel"
    Placeholders:
    - "Nota tambahan" → "Additional notes"
    - "Min 10 aksara" → "Min 10 characters"

    Toasts (8 Malay toasts — translate all found):
    - Any toast.success / toast.error with Malay text → English equivalent
    - Typical: "Diluluskan" → "Approved", "Ditolak" → "Rejected", "Gagal" prefix → "Failed to..."

    Any other Malay visible strings found during full file read → translate.

    ---

    PatientRegistry.tsx replacements:

    date-fns locale:
    - Remove import { ms } from "date-fns/locale"
    - Remove { locale: ms } from all formatDistanceToNow() calls

    Page title / description: translate any Malay heading or description text

    Search/button:
    - Search placeholder → English
    - Any button labels → English

    Dialog label: "Tambah sebagai pesakit baharu?" → "Add as new patient?"

    Card/table data labels:
    - "Ubat Semasa" → "Current Drug"
    - "Kunjungan Terakhir" → "Last Visit"
    - "Kuantiti Terkini" → "Latest Quantity"

    History table column headers:
    - "Tarikh" → "Date"
    - "Ubat" → "Drug"
    - "Kuantiti" → "Quantity"
    - "Kaedah" → "Method"
    - "Pegawai" → "Officer"
    - "Stok Selepas" → "Stock After"

    Toasts (2 Malay toasts):
    - Translate all toast strings found in file

    Any other Malay visible strings found during full file read → translate.
  </action>
  <verify>
    <automated>npx vitest run 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -n "locale.*ms\|import.*ms.*date-fns" src/pages/SpecialistDashboard.tsx returns zero matches
    - grep -n "locale.*ms\|import.*ms.*date-fns" src/pages/PatientRegistry.tsx returns zero matches
    - grep -n "Menunggu.*Ubat\|Menunggu.*Antibiotik\|Diluluskan Hari\|Ditolak Hari\|Ubat Kawalan\|Nota tambahan\|Min 10 aksara" src/pages/SpecialistDashboard.tsx returns zero matches
    - grep -n "Ubat Semasa\|Kunjungan Terakhir\|Kuantiti Terkini\|Tambah sebagai pesakit\|Kaedah\|Stok Selepas" src/pages/PatientRegistry.tsx returns zero matches
    - npx vitest run exits 0
  </acceptance_criteria>
  <done>SpecialistDashboard.tsx and PatientRegistry.tsx contain no Malay user-visible strings. date-fns ms locale removed from both. All tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Translate DoctorRequest.tsx, AntibioticForm.tsx, RoleManagement.tsx</name>
  <files>src/pages/DoctorRequest.tsx, src/pages/AntibioticForm.tsx, src/pages/RoleManagement.tsx</files>
  <read_first>
    - src/pages/DoctorRequest.tsx (read in full — Zod messages, form labels, placeholders, badge text, alert text, success screen, button text, toast)
    - src/pages/AntibioticForm.tsx (read in full — back button, placeholder, toasts)
    - src/pages/RoleManagement.tsx (read in full — page title, description, ROLE_LABELS constant, group titles, action buttons, toasts)
    - src/pages/DoctorRequest.test.tsx (read to understand what English strings the test expects — your output MUST satisfy these)
  </read_first>
  <behavior>
    DoctorRequest.tsx test assertions that must pass after this task:
    - screen.getByText("Patient Name *") renders
    - screen.getByText("Drug *") renders
    - Submit button renders (find by role or text)
    - Zod trigger: submit without patient name → screen.findByText("Patient name is required")
    - Zod trigger: submit without drug → screen.findByText("Please select a drug")
  </behavior>
  <action>
    DoctorRequest.tsx replacements:

    Zod schema messages (CRITICAL — these feed DoctorRequest.test.tsx assertions):
    - Find Zod schema (z.object({...})) in the file
    - "Nama pesakit diperlukan" → "Patient name is required"
    - "No. IC diperlukan" (or similar) → "IC number is required"
    - "Sila pilih ubat" → "Please select a drug"
    - "Kuantiti diperlukan" (or similar) → "Quantity is required"
    - Any remaining Malay Zod messages → translate

    Field labels:
    - "Nama Pesakit *" → "Patient Name *"
    - "No. IC Pesakit *" → "Patient IC No. *"
    - "Ubat *" → "Drug *"
    - "Kuantiti *" → "Quantity *"

    Placeholders:
    - "Nama penuh pesakit" → "Patient full name"
    - "Cari ubat..." → "Search drugs..."
    - "Tiada ubat dijumpai." → "No drugs found."

    Badge: "Perlu Kelulusan Pakar" → "Requires Specialist Approval"

    Alert text: "Ubat ini memerlukan kelulusan pakar..." → "This drug requires specialist approval before pharmacist processing."

    Success screen:
    - "Permintaan Berjaya Dihantar!" → "Request Submitted Successfully!"
    - "Pesakit" label → "Patient"
    - "Ubat" label → "Drug"
    - "Kuantiti" label → "Quantity"
    - "Status" label → "Status" (unchanged)
    - Badge: "Menunggu kelulusan pakar" → "Awaiting specialist approval"
    - Badge: "Menunggu pengesahan farmasis" → "Awaiting pharmacist confirmation"
    - Button: "Hantar Permintaan Baharu" → "Submit New Request"
    - Button: "Tukar Ubat Sahaja" → "Change Drug Only"

    Card title: "Permintaan Pengeluaran Ubat" → "Drug Dispensing Request"
    Card description: "Isi maklumat pesakit dan ubat yang diperlukan" → "Enter patient and drug details"

    Toast: toast.error("Gagal menghantar permintaan") → toast.error("Failed to submit request")

    Button: "Hantar Permintaan" (submit) → "Submit Request"

    ---

    AntibioticForm.tsx replacements:
    - Back button: "Kembali ke Pilihan Permohonan" → "Back to Request Options"
    - Placeholder: "Nama penuh" → "Full name"
    - Toast: toast.error("Sila lengkapkan Nama, IC dan Diagnosis") → toast.error("Please complete Name, IC and Diagnosis")
    - Toast: toast.error("Gagal menghantar borang") → toast.error("Failed to submit form")
    - Any other Malay visible strings found → translate

    ---

    RoleManagement.tsx replacements:
    Page title: "Pengurusan Peranan" → "Role Management"
    Description: "Tetapkan peranan kepada pengguna berdaftar." → "Assign roles to registered users."

    ROLE_LABELS constant (this is a JS object used for display — translate the values):
    ROLE_LABELS = { pharmacist: "Jurufarmasit", doctor: "Doktor", specialist: "Pakar" }
    → ROLE_LABELS = { pharmacist: "Pharmacist", doctor: "Doctor", specialist: "Specialist" }

    Group titles: "Doktor" → "Doctor", "Pakar" → "Specialist"
    Action buttons: "Jadikan Pakar" → "Make Specialist", "Jadikan Doktor" → "Make Doctor"

    Toasts:
    - "Peranan berjaya dikemas kini." → "Role updated successfully."
    - "Gagal mengemas kini peranan." → "Failed to update role."
    - Any other Malay toasts → English

    After all three files are edited, run: npx vitest run src/pages/DoctorRequest.test.tsx
    Tests must be GREEN.
  </action>
  <verify>
    <automated>npx vitest run src/pages/DoctorRequest.test.tsx 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - npx vitest run src/pages/DoctorRequest.test.tsx exits 0 (GREEN)
    - grep -n "Nama pesakit\|No. IC diperlukan\|Sila pilih ubat\|Nama Pesakit\|No. IC Pesakit\|Ubat \*\|Cari ubat\|Tiada ubat\|Perlu Kelulusan\|Ubat ini memerlukan\|Permintaan Berjaya\|Menunggu kelulusan\|Hantar Permintaan Baharu\|Tukar Ubat\|Permintaan Pengeluaran\|Isi maklumat\|Gagal menghantar" src/pages/DoctorRequest.tsx returns zero matches
    - grep -n "Kembali ke Pilihan\|Nama penuh\|Sila lengkapkan Nama\|Gagal menghantar borang" src/pages/AntibioticForm.tsx returns zero matches
    - grep -n "Pengurusan Peranan\|Jurufarmasit\|Jadikan Pakar\|Jadikan Doktor\|berjaya dikemas kini\|Gagal mengemas" src/pages/RoleManagement.tsx returns zero matches
    - npx vitest run exits 0 (all 4 scaffold tests now GREEN)
  </acceptance_criteria>
  <done>DoctorRequest.test.tsx passes GREEN (ENGL-04 Zod confirmed). All 3 files contain no Malay user-visible strings. npx vitest run exits 0 — all 4 scaffold tests are now GREEN.</done>
</task>

</tasks>

<verification>
After both tasks:
1. `npx vitest run` exits 0 — all 4 scaffold tests (AppSidebar, DrugFormDialog, Index, DoctorRequest) are GREEN
2. `grep -r "locale.*ms\|import.*ms.*date-fns" src/pages/SpecialistDashboard.tsx src/pages/PatientRegistry.tsx` returns zero matches
3. `npx vitest run src/pages/DoctorRequest.test.tsx` exits 0 (ENGL-04 confirmed)
</verification>

<success_criteria>
- All 4 scaffold tests from Plan 01 are GREEN (ENGL-01 through ENGL-04 confirmed by automated tests)
- 5 files in this plan contain zero Malay user-visible strings
- date-fns ms locale removed from SpecialistDashboard and PatientRegistry
- npx vitest run exits 0 across the full suite
</success_criteria>

<output>
After completion, create `.planning/phases/01-english-ui/01-05-SUMMARY.md` documenting:
- Each file translated
- Confirmation that all 4 scaffold tests are GREEN
- Full test suite status
- Any Malay strings found that were not in the research inventory
</output>
