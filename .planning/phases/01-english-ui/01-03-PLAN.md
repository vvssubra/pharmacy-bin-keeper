---
phase: 01-english-ui
plan: 03
type: execute
wave: 3
depends_on:
  - "01-02"
files_modified:
  - src/pages/Index.tsx
  - src/pages/DrugMaster.tsx
  - src/pages/BinCard.tsx
  - src/pages/DrugLedger.tsx
autonomous: true
requirements:
  - ENGL-01
  - ENGL-03
  - ENGL-05

must_haves:
  truths:
    - "Dashboard (Index) shows English stat card labels, column headers, status badges, and activity labels"
    - "Drug Master page shows English search placeholder, buttons, column headers, and deactivation dialog"
    - "BinCard shows English field labels, storage location labels, and bin card table headers"
    - "DrugLedger shows English filter labels, column headers, and pagination text"
  artifacts:
    - path: "src/pages/Index.tsx"
      provides: "English status badges (CRITICAL, LOW, EXCESS), column headers, stat labels"
      contains: "Critical Stock Alert"
    - path: "src/pages/DrugMaster.tsx"
      provides: "English drug master UI"
      contains: "Add Drug"
    - path: "src/pages/BinCard.tsx"
      provides: "English bin card labels"
      contains: "Stock Storage Location"
    - path: "src/pages/DrugLedger.tsx"
      provides: "English ledger column headers and filters"
      contains: "No transactions found"
  key_links:
    - from: "src/pages/Index.tsx"
      to: "src/pages/Index.test.tsx"
      via: "test imports component"
      pattern: "CRITICAL"
---

<objective>
Translate all Malay user-visible strings in the dashboard and drug inventory pages. After this plan, the Index.test.tsx from Plan 01 must turn GREEN.

Purpose: Complete the pharmacist/admin core UI pages — dashboard, drug master, bin card, and ledger.
Output: 4 pages fully in English; Index.test.tsx passes.
</objective>

<execution_context>
@/Users/subramaniampalaniapan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/subramaniampalaniapan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-english-ui/01-RESEARCH.md
@.planning/phases/01-english-ui/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Translate Index.tsx (Dashboard)</name>
  <files>src/pages/Index.tsx</files>
  <read_first>
    - src/pages/Index.tsx (read in full — this file has date-fns locale removal, status badges, stat cards, column headers, activity labels)
    - src/pages/Index.test.tsx (read to understand what English strings the test expects — your output MUST satisfy these)
  </read_first>
  <behavior>
    Index.tsx — complete string replacement list:
    Heading/subtitle:
    - "Gambaran keseluruhan stok semasa — {today}" → "Current stock overview — {today}"
    date-fns locale (CRITICAL — must remove both import AND usage):
    - Remove: import { ms } from "date-fns/locale"
    - Remove: { locale: ms } from every format() call — e.g. format(new Date(), "d MMMM yyyy", { locale: ms }) → format(new Date(), "d MMMM yyyy")
    - Remove: { locale: ms } from every formatDistanceToNow() call (two occurrences)
    Alert:
    - "Amaran Stok Kritikal" → "Critical Stock Alert"
    - "{n} ubat memerlukan perhatian segera..." → "{n} drug(s) require immediate attention..."
    - aria-label="Tutup amaran" → aria-label="Dismiss alert"
    Stat card labels:
    - "Kritikal" → "Critical"
    - "Rendah" → "Low"
    - "Lebihan" → "Excess"
    - "ubat" (below the count number) → "drug(s)"
    Table section:
    - "Status Stok Ubat" → "Drug Stock Status"
    Column headers:
    - "Nama Ubat" → "Drug Name"
    - "Baki" → "Balance"
    - "Kemaskini" → "Last Updated"
    - "Tindakan" → "Actions"
    Status badge values (these are computed/displayed strings, NOT DB values):
    - "KRITIKAL" → "CRITICAL"
    - "RENDAH" → "LOW"
    - "LEBIHAN" → "EXCESS"
    - "TIADA PARAS" → "NO LEVEL"
    Recent activity section:
    - "Aktiviti Terkini" → "Recent Activity"
    - "Lihat Semua" → "View All"
    - Activity type labels: "Terimaan" → "Receipt", "Keluaran" → "Dispensed"
    Recent dispensing section:
    - "Pengeluaran Terkini" → "Recent Dispensing"
    - Column: "Ubat" → "Drug", "Pesakit" → "Patient", "Pegawai" → "Officer", "Masa" → "Time"
    Pending requests section:
    - "Permintaan Menunggu" → "Pending Requests"
    - "Ubat Kawalan" badge → "Controlled Drug"
    - "{n} menunggu" → "{n} pending"
    - "Proses →" → "Process →"
    - "Borang Antibiotik" → "Antibiotic Form"
    - "{n} perlu pengesahan" → "{n} awaiting confirmation"
    - "Semak →" → "Review →"

    DO NOT change: database column names, Supabase query filter values (.eq("jenis", "terimaan") etc.), TypeScript object keys.
  </behavior>
  <action>
    1. Read src/pages/Index.tsx in full.
    2. Apply ALL string replacements listed in behavior. Work section by section top-to-bottom.
    3. For date-fns: find import { ms } from "date-fns/locale" and remove the entire import line. Find every { locale: ms } occurrence in format() and formatDistanceToNow() calls and remove just that option object property (preserve other options like { addSuffix: true }).
    4. After edits, mentally scan the file for any remaining Malay words not in the list above.
    5. Run test: npx vitest run src/pages/Index.test.tsx
  </action>
  <verify>
    <automated>npx vitest run src/pages/Index.test.tsx 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - npx vitest run src/pages/Index.test.tsx exits 0
    - grep -n "Kritikal\|Rendah\|Lebihan\|Amaran\|Pesakit\|Pegawai\|Tindakan\|Kemaskini\|Lihat Semua\|Pengeluaran\|ubat memerlukan\|locale.*ms\|import.*ms.*date-fns" src/pages/Index.tsx returns zero matches
    - grep -n "{ locale: ms }" src/pages/Index.tsx returns zero matches
  </acceptance_criteria>
  <done>Index.test.tsx passes GREEN. No Malay user-visible strings remain in src/pages/Index.tsx. date-fns ms locale import and all usages removed.</done>
</task>

<task type="auto">
  <name>Task 2: Translate DrugMaster.tsx, BinCard.tsx, DrugLedger.tsx</name>
  <files>src/pages/DrugMaster.tsx, src/pages/BinCard.tsx, src/pages/DrugLedger.tsx</files>
  <read_first>
    - src/pages/DrugMaster.tsx (read in full — search placeholder, buttons, column headers, dialogs, toasts)
    - src/pages/BinCard.tsx (read in full — field labels, storage location labels, bin card table, getStockLevel function returning status strings)
    - src/pages/DrugLedger.tsx (read in full — filter labels, column headers, select items, pagination text, empty states)
  </read_first>
  <action>
    DrugMaster.tsx replacements:
    - "Senarai ubat yang dipantau (KEW.PS-3)" → "Monitored drug list (KEW.PS-3)"
    - "Tambah Ubat" button → "Add Drug"
    - placeholder "Cari ubat..." → "Search drugs..."
    Column headers:
    - "Kumpulan" → "Group"
    - "Paras Stok" → "Stock Levels"
    - "Baki Awal" → "Opening Balance"
    Empty states:
    - "Memuatkan..." → "Loading..."
    - "Tiada ubat ditemui." → "No drugs found."
    - "Klik 'Tambah Ubat' untuk mula." → "Click 'Add Drug' to start."
    Badge:
    - "Belum Ditetapkan" → "Not Set"
    Buttons:
    - "Set Baki" → "Set Balance"
    Tooltips:
    - "Lihat Kad" → "View Card"
    - "Lihat Lejar" → "View Ledger"
    Toast: toast.success("Status dikemaskini") → toast.success("Status updated")
    Alert dialog:
    - "Nyahaktif ubat ini?" → "Deactivate this drug?"
    - "Ubat X akan dinyahaktifkan..." → "Drug X will be deactivated..."
    - "Batal" → "Cancel"
    - "Nyahaktif" → "Deactivate"

    BinCard.tsx replacements:
    Back button: "Senarai Ubat" → "Drug List"
    Loading: "Memuatkan..." → "Loading..."
    Empty: "Ubat tidak ditemui." → "Drug not found."
    Detail labels:
    - "Perihal Stok" → "Stock Description"
    - "No. Kod" → "Code No."
    - "Unit Pengukuran" → "Unit of Measure"
    - "Kumpulan" → "Group"
    Location section:
    - "Lokasi Penyimpanan Stok" → "Stock Storage Location"
    - "Gudang / Seksyen" → "Warehouse / Section"
    - "Baris" → "Row"
    - "Rak" → "Shelf"
    - "Tingkat" → "Level"
    - "Petak" → "Compartment"
    - "Kod Lokasi Penuh" → "Full Location Code"
    - "Paras Stok" → "Stock Levels"
    - "Baki Semasa" → "Current Balance"
    getStockLevel function — change the returned DISPLAY strings (not the input comparison values):
    - return "KRITIKAL" → return "CRITICAL"
    - return "RENDAH" → return "LOW"
    - return "LEBIHAN" → return "EXCESS"
    Filter label "Jenis" → "Type"
    Filter options display: "semua" option display → "All", "terimaan" display → "Receipt", "keluaran" display → "Dispensed"
    Toast: toast.info("Akan datang") → toast.info("Coming soon")
    Buttons: "Jana PDF" → "Generate PDF", "Tambah Terimaan" → "Add Receipt"
    BinCard table section header: "Perihal Stok" → "Stock Description"
    Mock data string "BAKI DIBAWA KE HADAPAN" → "BALANCE BROUGHT FORWARD" (if it appears as a literal string)

    DrugLedger.tsx replacements:
    Loading: "Memuatkan..." → "Loading..."
    Not found: "Ubat tidak dijumpai." → "Drug not found."
    Back: "Kembali" → "Back"
    Field labels:
    - "No. Kod:" → "Code No.:"
    - "Kumpulan:" → "Group:"
    - "Lokasi:" → "Location:"
    - "Paras Stok:" → "Stock Levels:"
    - "Baki Semasa" → "Current Balance"
    Filter labels: "Dari" → "From", "Hingga" → "To", "Jenis" → "Type", "Carian" → "Search"
    Filter placeholders: "Mula" → "Start", "Akhir" → "End"
    Select items: "Semua" → "All", "Terimaan" → "Receipt", "Keluaran" → "Dispensed"
    Column headers:
    - "Tarikh" → "Date"
    - "No. Rujukan" → "Ref. No."
    - "Terima / Keluar Kepada" → "Received / Dispensed To"
    - "Terimaan" (column header) → "Receipts"
    - "Keluaran" (column header) → "Dispensed"
    - "Baki" → "Balance"
    - "Pegawai" → "Officer"
    - "Sumber" → "Source"
    Sub-headers:
    - "Seunit (RM)" → "Unit Price (RM)"
    - "Jumlah (RM)" → "Total (RM)"
    Empty states:
    - "Tiada transaksi dijumpai" → "No transactions found"
    - "Mulakan dengan menetapkan baki awal" → "Start by setting an opening balance"
    Pagination: "Menunjukkan 1-{n} daripada {n} transaksi" → "Showing 1-{n} of {n} transactions"
    (Note: DrugLedger.tsx likely does NOT import date-fns locale — but if found, remove { locale: ms } and its import.)

    IMPORTANT for all 3 files: Read the complete file before editing. The lists above are from the research inventory — additional Malay strings may exist. Translate ALL visible strings found.
  </action>
  <verify>
    <automated>npx vitest run 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -n "Cari ubat\|Tambah Ubat\|Memuatkan\|Tiada ubat\|Belum Ditetapkan\|Lihat Kad\|Lihat Lejar\|Nyahaktif\|Status dikemaskini" src/pages/DrugMaster.tsx returns zero matches
    - grep -n "Senarai Ubat\|Perihal Stok\|Unit Pengukuran\|Gudang.*Seksyen\|Baris\|Rak\|Tingkat\|Petak\|Baki Semasa\|KRITIKAL\|RENDAH\|LEBIHAN\|Akan datang\|Jana PDF\|Tambah Terimaan" src/pages/BinCard.tsx returns zero matches
    - grep -n "Memuatkan\|Ubat tidak\|Kembali\|Tarikh\|Pegawai\|Baki\|Sumber\|Semua\|Terimaan\|Keluaran\|Tiada transaksi\|daripada.*transaksi" src/pages/DrugLedger.tsx returns zero matches
    - npx vitest run exits 0 (all tests pass)
  </acceptance_criteria>
  <done>DrugMaster.tsx, BinCard.tsx, and DrugLedger.tsx contain no Malay user-visible strings. All existing tests pass.</done>
</task>

</tasks>

<verification>
After both tasks:
1. `npx vitest run src/pages/Index.test.tsx` exits 0 (GREEN — ENGL-03 confirmed)
2. `npx vitest run` exits 0 (no regressions)
3. `grep -r "Kritikal\|Rendah\|Lebihan\|Tiada ubat\|Memuatkan\|Baki Semasa\|Perihal Stok" src/pages/Index.tsx src/pages/DrugMaster.tsx src/pages/BinCard.tsx src/pages/DrugLedger.tsx` returns zero matches
</verification>

<success_criteria>
- Index.test.tsx passes (ENGL-03 confirmed for status badges)
- All 4 files have zero Malay user-visible strings
- date-fns ms locale removed from Index.tsx
- npx vitest run exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/01-english-ui/01-03-SUMMARY.md` documenting:
- Each file translated and key changes made
- Index.test.tsx GREEN confirmation
- Any Malay strings found that were not in the research inventory
</output>
