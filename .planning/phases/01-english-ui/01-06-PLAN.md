---
phase: 01-english-ui
plan: 06
type: execute
wave: 5
depends_on:
  - "01-05"
files_modified: []
autonomous: false
requirements:
  - ENGL-01
  - ENGL-02
  - ENGL-03
  - ENGL-04
  - ENGL-05

must_haves:
  truths:
    - "All 4 unit tests pass (ENGL-01 through ENGL-04 confirmed by automation)"
    - "Primary ENGL-05 grep returns zero matches"
    - "Secondary broad grep returns zero matches (or only false positives in non-display code)"
    - "date-fns ms locale import is absent from all 5 affected files"
    - "Human confirms the running app shows English text in browser"
  artifacts:
    - path: "src/"
      provides: "Zero Malay user-visible strings across entire src/ directory"
  key_links:
    - from: "all src/ files"
      to: "ENGL-05 grep pattern"
      via: "zero-match requirement"
      pattern: "Ubat|Pesakit|Doktor|berjaya|gagal|Pilih|Cari|Kemaskini|Tambah"
---

<objective>
Final verification gate for Phase 1: English UI. Run automated grep checks, confirm all 4 unit tests are green, then verify visually in the running app. This is the phase completion checkpoint before /gsd:verify-work.

Purpose: Confirm the goal is achieved — "All visible text in the application is in English — no Malay strings survive in any page, component, toast, or validation message."
Output: Signed-off phase completion; green test suite; zero grep matches.
</objective>

<execution_context>
@/Users/subramaniampalaniapan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/subramaniampalaniapan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-english-ui/01-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full automated verification suite</name>
  <files></files>
  <read_first>
    - .planning/phases/01-english-ui/01-05-SUMMARY.md (read to understand current state before running checks)
  </read_first>
  <action>
    Run these checks in sequence. For each check, record the result (pass/fail) and any output:

    1. Full test suite:
       npx vitest run
       Expected: exits 0, all tests pass including the 4 scaffold tests

    2. Primary ENGL-05 grep (from ROADMAP success criteria):
       grep -r "Ubat\|Pesakit\|Doktor\|berjaya\|gagal\|Pilih\|Cari\|Kemaskini\|Tambah" src/ | grep -v "node_modules\|\.test\.\|RESEARCH\|PLAN\|SUMMARY\|VALIDATION"
       Expected: zero output (zero matches in non-test source files)

    3. Secondary broad grep (from research common pitfalls — catches strings not in primary pattern):
       grep -r "Memuatkan\|Tiada\|Selesai\|Sahkan\|Hantar\|Simpan\|Batal\|Semak\|Laporan\|Lejar\|Senarai\|Semua\|Paras\|Baki\|Tarikh\|Dari\|Hingga\|Mula\|Akhir" src/ | grep -v "node_modules\|\.test\.\|RESEARCH\|PLAN\|SUMMARY\|VALIDATION\|database\|query\|\.eq\|column\|schema\|type\|interface"
       Expected: zero output, or only false positives in non-display code (TypeScript identifiers, database column references)
       Note: Review any remaining matches — if they are in .eq("tarikh", ...) or TypeScript object keys, they are NOT display strings and can be ignored.

    4. date-fns locale check across all 5 affected files:
       grep -n "locale.*ms\|import.*ms.*date-fns\|from.*date-fns/locale" src/pages/Index.tsx src/pages/PharmacistFulfilment.tsx src/pages/Laporan.tsx src/pages/SpecialistDashboard.tsx src/pages/PatientRegistry.tsx
       Expected: zero matches

    5. Build check (ensure no TypeScript errors from the edits):
       npm run build 2>&1 | tail -30
       Expected: build succeeds with no errors (warnings acceptable)

    If any check fails, identify which file(s) contain the remaining Malay string(s) and fix them immediately before proceeding to the human checkpoint.
  </action>
  <verify>
    <automated>npx vitest run 2>&1 | grep -E "Tests|passed|failed|errors" | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - npx vitest run exits 0
    - Primary grep returns zero matches
    - date-fns locale grep returns zero matches
    - npm run build exits 0 (or non-zero only for pre-existing non-translation errors)
  </acceptance_criteria>
  <done>All automated checks pass. Zero Malay strings in primary grep. Zero date-fns locale usages. Build succeeds. Ready for human visual verification.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Phase 1: English UI — all 19 source files translated from Malay to English. Includes:
    - Navigation labels (AppSidebar)
    - Form field labels, placeholders, buttons (DrugFormDialog, Terimaan, DoctorRequest, etc.)
    - Status badges (Dashboard, BinCard)
    - Toast notifications and Zod validation messages (all pages)
    - Table column headers and data labels (DrugLedger, PharmacistFulfilment, etc.)
    - date-fns locale removed from 5 files (Index, PharmacistFulfilment, Laporan, SpecialistDashboard, PatientRegistry)
  </what-built>
  <how-to-verify>
    1. Start the dev server: npm run dev
    2. Open http://localhost:8080 in your browser
    3. Log in with any account

    Check these pages for any remaining Malay text:
    - Dashboard (/) — look at stat cards (Critical/Low/Excess), column headers, activity section
    - Drug Master (/drugs) — look at search placeholder, buttons, table headers
    - Bin Card (click "View Card" on any drug) — look at storage location labels
    - Drug Ledger (click "View Ledger" on any drug) — look at column headers, filter labels
    - Terimaan (/terimaan) — look at form field labels, select placeholders
    - Pharmacist Fulfilment (/fulfilment) — look at tab labels, card data labels
    - Sidebar — look at all nav item labels
    - Role Management — look at page title, role labels, action buttons

    If logged in as a doctor:
    - Doctor Request (/request/ubat) — look at form labels, submit button
    - Success screen after submitting a request — look at status badge text

    If logged in as a specialist:
    - Specialist Dashboard (/specialist) — look at stat cards, tab labels

    Report any page/component that still shows Malay text.
  </how-to-verify>
  <resume-signal>Type "approved" if all visible text is in English, or describe which page/component still shows Malay text (e.g. "Pharmacist Fulfilment tab still shows 'Menunggu Pengesahan'")</resume-signal>
</task>

</tasks>

<verification>
Phase 1 complete when:
1. npx vitest run exits 0 (all 4 scaffold tests GREEN)
2. Primary ENGL-05 grep returns zero matches
3. date-fns locale grep returns zero matches
4. npm run build succeeds
5. Human approves visual inspection
</verification>

<success_criteria>
- All 5 ROADMAP success criteria for Phase 1 are met:
  1. Every sidebar navigation label and page title displays in English
  2. Every form field label, placeholder, and button reads in English
  3. Every status badge value displays in English
  4. Every toast notification and inline error message appears in English, including Zod validation messages
  5. Every table column header and data label is in English; grep check returns zero matches
- npx vitest run exits 0
- Human approves visual verification
</success_criteria>

<output>
After completion, create `.planning/phases/01-english-ui/01-06-SUMMARY.md` documenting:
- Final grep results (zero matches confirmed)
- Test suite status (all 4 scaffold tests GREEN)
- Build status
- Human verification sign-off
- Phase 1 marked complete
</output>
