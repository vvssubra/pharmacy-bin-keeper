---
phase: 01-english-ui
plan: 04
type: execute
wave: 3
depends_on:
  - "01-02"
files_modified:
  - src/pages/Terimaan.tsx
  - src/pages/PharmacistFulfilment.tsx
  - src/pages/Laporan.tsx
autonomous: true
requirements:
  - ENGL-02
  - ENGL-04
  - ENGL-05

must_haves:
  truths:
    - "Terimaan (Receipt) page shows English Zod messages, form labels, placeholders, and toasts"
    - "Pharmacist Fulfilment page shows English tab labels, card data labels, action buttons, dialogs, and toasts"
    - "Reports (Laporan) page shows English section labels, filter labels, column headers, and quarter labels"
    - "date-fns ms locale removed from PharmacistFulfilment.tsx and Laporan.tsx"
  artifacts:
    - path: "src/pages/Terimaan.tsx"
      provides: "English receipt form UI"
      contains: "Receipt saved successfully"
    - path: "src/pages/PharmacistFulfilment.tsx"
      provides: "English fulfilment UI"
      contains: "Awaiting Confirmation"
    - path: "src/pages/Laporan.tsx"
      provides: "English reports UI"
      contains: "Quarterly Summary"
  key_links:
    - from: "src/pages/PharmacistFulfilment.tsx"
      to: "PharmacistFulfilment state"
      via: "date-fns locale removal"
      pattern: "formatDistanceToNow"
---

<objective>
Translate Terimaan (stock receipt), Pharmacist Fulfilment, and Laporan (Reports) pages. These are the most text-heavy pharmacist-facing pages. This plan runs parallel to Plan 03 (no file overlap).

Purpose: Complete the pharmacist workflow pages — receipt entry, request fulfilment, and reporting.
Output: 3 pages fully in English with date-fns locale removed from 2 of them.
</objective>

<execution_context>
@/Users/subramaniampalaniapan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/subramaniampalaniapan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-english-ui/01-RESEARCH.md
@.planning/phases/01-english-ui/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Translate Terimaan.tsx</name>
  <files>src/pages/Terimaan.tsx</files>
  <read_first>
    - src/pages/Terimaan.tsx (read in full — Zod schema messages, field labels, ComboBox placeholders, select items, toast strings)
  </read_first>
  <action>
    Read src/pages/Terimaan.tsx in full. Apply ALL string replacements:

    Zod schema validation messages (inside z.string(), z.number(), z.date() calls):
    - "Sila pilih ubat" → "Please select a drug"
    - "Tarikh diperlukan" → "Date is required"
    - "No. Rujukan diperlukan" → "Reference number is required"
    - "Kuantiti mestilah sekurang-kurangnya 1" → "Quantity must be at least 1"
    - "Harga mestilah >= 0" → "Price must be >= 0"

    Field labels:
    - "Ubat *" → "Drug *"
    - "Tarikh Terima *" → "Receipt Date *"
    - "No. Rujukan *" → "Reference No. *"
    - "Kuantiti *" → "Quantity *"
    - "Nama Pegawai" → "Officer Name"

    Placeholders:
    - "Pilih ubat..." → "Select drug..."
    - "Cari ubat..." → "Search drugs..."
    - "Tiada ubat dijumpai." → "No drugs found."
    - "Pilih tarikh" → "Select date"
    - "Pilih jenis" → "Select type"
    - "Masukkan nombor rujukan dokumen" → "Enter document reference number"
    - "Catatan tambahan (pilihan)" → "Additional notes (optional)"

    Select items (display text only — NOT the value prop):
    - "terimaan" display label → "Receipt" (keep value="terimaan")
    - "keluaran" display label → "Dispensed" (keep value="keluaran")

    Toast strings:
    - toast.success("Terimaan berjaya disimpan") → toast.success("Receipt saved successfully")
    - toast.success("Terimaan berjaya dikemaskini") → toast.success("Receipt updated successfully")
    - toast.error("Gagal menyimpan terimaan") → toast.error("Failed to save receipt")
    - toast.error("Gagal mengemaskini terimaan") → toast.error("Failed to update receipt")

    Page title / heading (if any Malay headings exist in the file): translate to English.

    DO NOT change: .eq("jenis", "terimaan"), .eq("jenis", "keluaran"), database column names (kuantiti, tarikh, jenis, etc.), TypeScript object property keys that map to DB columns.
  </action>
  <verify>
    <automated>npx vitest run 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -n "Sila pilih\|Tarikh diperlukan\|Rujukan diperlukan\|sekurang-kurangnya\|Pilih ubat\|Cari ubat\|Tiada ubat\|Pilih tarikh\|Pilih jenis\|nombor rujukan\|Catatan tambahan\|berjaya disimpan\|berjaya dikemaskini\|Gagal menyimpan\|Gagal mengemaskini" src/pages/Terimaan.tsx returns zero matches
    - npx vitest run exits 0
  </acceptance_criteria>
  <done>Terimaan.tsx contains no Malay user-visible strings. All Zod messages, labels, placeholders, and toasts are in English. npx vitest run exits 0.</done>
</task>

<task type="auto">
  <name>Task 2: Translate PharmacistFulfilment.tsx and Laporan.tsx</name>
  <files>src/pages/PharmacistFulfilment.tsx, src/pages/Laporan.tsx</files>
  <read_first>
    - src/pages/PharmacistFulfilment.tsx (read in full — this is the largest file; tabs, card labels, buttons, dialogs, toasts, date-fns locale)
    - src/pages/Laporan.tsx (read in full — section labels, filters, column headers, quarter labels, date-fns locale)
  </read_first>
  <action>
    PharmacistFulfilment.tsx replacements:

    date-fns locale (CRITICAL):
    - Remove import { ms } from "date-fns/locale"
    - Remove { locale: ms } from ALL formatDistanceToNow() calls (4 occurrences)

    Page title: "Permintaan Untuk Diselesaikan" → "Requests to Fulfil"
    Sub-heading: "Klik Selesai untuk mengesahkan pengeluaran dan tolak stok" → "Click Complete to confirm dispensing and deduct stock"

    Tabs:
    - "Menunggu Pengesahan" → "Awaiting Confirmation"
    - "Selesai Hari Ini" → "Completed Today"
    - "Borang Antibiotik" → "Antibiotic Forms"
    Nested tabs:
    - "Perlu Pengesahan" → "Needs Confirmation"
    - "Telah Disahkan Hari Ini" → "Confirmed Today"

    Card badges:
    - "Diluluskan Pakar" → "Specialist Approved"
    - "Ditangguh" → "Deferred"

    Card data labels:
    - "Pesakit" → "Patient"
    - "Kuantiti" → "Quantity"
    - "Doktor" → "Doctor"
    - "Stok Semasa" → "Current Stock"

    Inline messages:
    - "Stok di bawah paras minimum" → "Stock below minimum level"
    - "Stok Habis — Tambah Terimaan dahulu" → "Out of Stock — Add Receipt first"
    - "{n} selepas selesai" → "{n} after completion"

    Buttons:
    - "Tindakan Lain" → "Other Actions"
    - "Tolak" → "Reject"
    - "Tangguh ke esok" → "Defer to tomorrow"
    - "Selesai" → "Complete"

    Fulfilled table column headers:
    - "Masa" → "Time"
    - "Pesakit" → "Patient"
    - "Kuantiti" → "Quantity"
    - "Baki Selepas" → "Balance After"
    - "Pegawai" → "Officer"

    Antibiotic card labels (note: "IC", "Diagnosis", "Unit", "Antibiotik" — "Antibiotik" → "Antibiotic", others are already English or proper nouns):
    - "Antibiotik" label → "Antibiotic"

    Antibiotic buttons:
    - "Semak Borang" → "Review Form"
    - "Akui Terima" → "Acknowledge"

    Acked table column headers:
    - "Masa" → "Time"
    - "Pesakit" → "Patient"
    - "Diagnosis" → "Diagnosis" (unchanged)
    - "Antibiotik" (column header) → "Antibiotic"

    Empty states:
    - "Tiada permintaan menunggu" → "No pending requests"
    - "Tiada pengeluaran hari ini" → "No dispensing today"
    - "Tiada borang antibiotik menunggu pengesahan" → "No antibiotic forms awaiting confirmation"
    - "Tiada borang disahkan hari ini" → "No forms confirmed today"

    Dialog — confirm dispensing:
    - "Sahkan Pengeluaran" → "Confirm Dispensing"
    - Any Malay confirmation text → English
    - "Batal" → "Cancel"
    - "Memproses..." → "Processing..."
    - "Sahkan & Selesai" → "Confirm & Complete"

    Reject dialog:
    - "Tolak Permintaan" → "Reject Request"
    - "Sebab Penolakan *" → "Rejection Reason *"
    - "Min 10 aksara" → "Min 10 characters"

    Acknowledge dialog:
    - "Akui Borang Antibiotik" → "Acknowledge Antibiotic Form"

    Toasts (5 Malay toasts — translate all found in file):
    - Any toast with Malay text → translate to English equivalent

    ---

    Laporan.tsx replacements:

    date-fns locale:
    - Remove import { ms } from "date-fns/locale" (if present)
    - Remove { locale: ms } from any format() / formatDistanceToNow() calls

    Section labels:
    - "Ubat" (filter label) → "Drug"
    - "Suku" → "Quarter"
    - "Tahun" → "Year"
    - "Dari Tarikh" → "From Date"
    - "Hingga Tarikh" → "To Date"

    Card description: "Jana PDF kad stok dalam format KEW.PS-3..." → "Generate stock card PDF in KEW.PS-3 format..."
    Card title: "Ringkasan Suku Tahun" → "Quarterly Summary"
    Card description: "Agregat terimaan dan keluaran stok mengikut suku tahun" → "Aggregate stock receipts and dispensing by quarter"

    Quarter select items (display text only):
    - "Semua Suku" → "All Quarters"
    - "Q1 Jan–Mac" → "Q1 Jan–Mar"
    - "Q2 Apr–Jun" → "Q2 Apr–Jun" (unchanged)
    - "Q3 Jul–Sep" → "Q3 Jul–Sep" (unchanged)
    - "Q4 Okt–Dis" → "Q4 Oct–Dec"

    Buttons:
    - "Jana Laporan" → "Generate Report"
    - "Jana PDF" → "Generate PDF"

    Column headers:
    - "Ubat" → "Drug"
    - "Terima" → "Received"
    - "Keluar" → "Dispensed"
    - "Baki" → "Balance"

    PDF note: "PDF akan mengandungi Bahagian A dan Bahagian B..." → "PDF will contain Part A and Part B..."

    Placeholders:
    - "Cari ubat..." → "Search drugs..."
    - "Tiada ubat dijumpai." → "No drugs found."

    Read each file fully — translate ALL Malay visible strings found, not just the listed ones.
  </action>
  <verify>
    <automated>npx vitest run 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -n "locale.*ms\|import.*ms.*date-fns" src/pages/PharmacistFulfilment.tsx returns zero matches
    - grep -n "locale.*ms\|import.*ms.*date-fns" src/pages/Laporan.tsx returns zero matches
    - grep -n "Menunggu Pengesahan\|Selesai Hari Ini\|Borang Antibiotik\|Diluluskan Pakar\|Ditangguh\|Stok Habis\|selepas selesai\|Tolak Permintaan\|Sebab Penolakan\|Akui Terima\|Akui Borang\|Tiada permintaan\|Tiada pengeluaran\|Tiada borang" src/pages/PharmacistFulfilment.tsx returns zero matches
    - grep -n "Suku Tahun\|Dari Tarikh\|Hingga Tarikh\|Jana Laporan\|Jana PDF\|Semua Suku\|Jan–Mac\|Okt–Dis\|Bahagian A" src/pages/Laporan.tsx returns zero matches
    - npx vitest run exits 0
  </acceptance_criteria>
  <done>PharmacistFulfilment.tsx and Laporan.tsx contain no Malay user-visible strings. date-fns ms locale removed from both. npx vitest run exits 0.</done>
</task>

</tasks>

<verification>
After both tasks:
1. `npx vitest run` exits 0
2. `grep -r "locale.*ms\|import.*ms.*date-fns" src/pages/PharmacistFulfilment.tsx src/pages/Laporan.tsx` returns zero matches
3. `grep -r "Menunggu Pengesahan\|Sahkan Pengeluaran\|Suku Tahun\|Jana Laporan\|Terimaan berjaya\|Gagal menyimpan" src/pages/Terimaan.tsx src/pages/PharmacistFulfilment.tsx src/pages/Laporan.tsx` returns zero matches
</verification>

<success_criteria>
- Terimaan.tsx, PharmacistFulfilment.tsx, and Laporan.tsx contain zero Malay user-visible strings
- date-fns ms locale removed from PharmacistFulfilment.tsx and Laporan.tsx
- All Zod messages and toast strings in Terimaan are in English
- npx vitest run exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/01-english-ui/01-04-SUMMARY.md` documenting:
- Each file translated and key changes made
- Confirmed date-fns locale removal from both files
- Any Malay strings found that were not in the research inventory
</output>
