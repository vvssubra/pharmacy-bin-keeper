---
phase: 01-english-ui
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - src/components/AppSidebar.tsx
  - src/components/DrugFormDialog.tsx
  - src/components/OpeningBalanceDialog.tsx
  - src/components/NoPermission.tsx
  - src/components/AntibioticFormReadOnly.tsx
  - src/pages/Login.tsx
  - src/pages/DoctorLanding.tsx
autonomous: true
requirements:
  - ENGL-01
  - ENGL-02
  - ENGL-04

must_haves:
  truths:
    - "Sidebar navigation labels render in English in the browser"
    - "DrugFormDialog shows English field labels, button text, and Zod validation messages"
    - "OpeningBalanceDialog shows English labels and toasts"
    - "NoPermission, AntibioticFormReadOnly, Login, and DoctorLanding contain no Malay user-visible strings"
  artifacts:
    - path: "src/components/AppSidebar.tsx"
      provides: "English nav labels"
      contains: "New Requests"
    - path: "src/components/DrugFormDialog.tsx"
      provides: "English form labels + Zod messages"
      contains: "Drug name is required"
    - path: "src/components/OpeningBalanceDialog.tsx"
      provides: "English labels and toasts"
      contains: "Opening balance saved"
    - path: "src/pages/Login.tsx"
      provides: "English login page text"
  key_links:
    - from: "src/components/AppSidebar.tsx"
      to: "src/components/AppSidebar.test.tsx"
      via: "test imports component"
      pattern: "New Requests"
    - from: "src/components/DrugFormDialog.tsx"
      to: "src/components/DrugFormDialog.test.tsx"
      via: "test imports component"
      pattern: "Drug name is required"
---

<objective>
Translate all Malay user-visible strings to English across shared components and small pages. This is Wave 2 of the translation. After this plan, the AppSidebar and DrugFormDialog tests from Plan 01 must turn GREEN.

Purpose: Translate the foundation layer — nav, dialogs, auth page, and role-agnostic components.
Output: 7 files fully translated to English; AppSidebar.test and DrugFormDialog.test pass.
</objective>

<execution_context>
@/Users/subramaniampalaniapan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/subramaniampalaniapan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-english-ui/01-RESEARCH.md
@.planning/phases/01-english-ui/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Translate AppSidebar and DrugFormDialog</name>
  <files>src/components/AppSidebar.tsx, src/components/DrugFormDialog.tsx</files>
  <read_first>
    - src/components/AppSidebar.tsx (read in full before touching — see current Malay strings, nav item structure)
    - src/components/DrugFormDialog.tsx (read in full before touching — see all field labels, Zod schema, button text, toast calls)
    - src/components/AppSidebar.test.tsx (read to understand what English strings the tests expect — your output MUST satisfy these)
    - src/components/DrugFormDialog.test.tsx (read to understand what English strings the tests expect)
  </read_first>
  <behavior>
    AppSidebar.tsx — string replacements:
    - "Permintaan Baharu" → "New Requests"
    - "Pesakit" → "Patients"
    - "Laporan" → "Reports"
    - "Pengurusan Peranan" → "Role Management"
    - "Kawalan Ubat" (brand text) → "Drug Control"
    - Any other Malay labels found in the file

    DrugFormDialog.tsx — string replacements:
    Dialog titles:
    - "Edit Ubat" → "Edit Drug"
    - "Tambah Ubat" → "Add Drug"
    Dialog descriptions:
    - "Kemaskini maklumat ubat." → "Update drug information."
    - "Isi maklumat ubat baharu." → "Fill in new drug details."
    Field labels:
    - "Nama Ubat *" → "Drug Name *"
    - "No. Kod" → "Code No."
    - "Unit Pengukuran" → "Unit of Measure"
    - "Kumpulan" → "Group"
    - "Pergerakan" → "Movement"
    - "Lokasi Penyimpanan" → "Storage Location"
    - "Gudang/Seksyen" → "Warehouse/Section"
    - "Kod Lokasi Penuh" → "Full Location Code"
    - "Paras Stok (Tahun Semasa)" → "Stock Levels (Current Year)"
    - "Menokok/Reorder" → "Reorder"
    - "Maksimum" → "Maximum"
    Placeholders:
    - "cth: A/KK" → "e.g. A/KK"
    Buttons:
    - "Batal" → "Cancel"
    - "Menyimpan..." → "Saving..."
    - "Simpan" → "Save"
    Zod messages (inside z.string(), z.number() schema definitions):
    - "Nama ubat diperlukan" → "Drug name is required"
    Toasts:
    - toast.success("Ubat dikemaskini") → toast.success("Drug updated")
    - toast.success("Ubat ditambah") → toast.success("Drug added")
    form.setError message:
    - "Nama ubat sudah wujud" → "Drug name already exists"

    DO NOT change: database column names (drug_name, unit_pengukuran, etc.), Supabase query values (.eq("status", "...")), TypeScript object property keys that map to DB columns.
  </behavior>
  <action>
    1. Read src/components/AppSidebar.tsx in full. Apply every string replacement listed in behavior. Remove any Malay strings not listed but found during read.
    2. Read src/components/DrugFormDialog.tsx in full. Apply every string replacement listed in behavior. After replacements, verify no Malay text remains by scanning the file mentally — look for any remaining Malay words.
    3. Run the test to verify GREEN: npx vitest run src/components/AppSidebar.test.tsx src/components/DrugFormDialog.test.tsx
  </action>
  <verify>
    <automated>npx vitest run src/components/AppSidebar.test.tsx src/components/DrugFormDialog.test.tsx 2>&1 | tail -20</automated>
  </verify>
  <done>AppSidebar.test.tsx and DrugFormDialog.test.tsx both pass (green). No "Permintaan Baharu", "Tambah Ubat", "Nama Ubat", "Simpan", or "Nama ubat diperlukan" remain in these two files.</done>
</task>

<task type="auto">
  <name>Task 2: Translate OpeningBalanceDialog, NoPermission, AntibioticFormReadOnly, Login, DoctorLanding</name>
  <files>
    src/components/OpeningBalanceDialog.tsx,
    src/components/NoPermission.tsx,
    src/components/AntibioticFormReadOnly.tsx,
    src/pages/Login.tsx,
    src/pages/DoctorLanding.tsx
  </files>
  <read_first>
    - src/components/OpeningBalanceDialog.tsx (read in full — find all Malay labels and toast strings)
    - src/components/NoPermission.tsx (read in full — small file, audit all text)
    - src/components/AntibioticFormReadOnly.tsx (read in full — find all Malay field labels)
    - src/pages/Login.tsx (read in full — find all Malay form labels, button text, error messages)
    - src/pages/DoctorLanding.tsx (read in full — small file ~59 lines, audit all text)
  </read_first>
  <action>
    For each file:
    1. Read the full file
    2. Find ALL Malay user-visible strings (labels, placeholders, buttons, toasts, error messages, headings, descriptions)
    3. Replace with English equivalents

    Known replacements for OpeningBalanceDialog.tsx:
    - toast.success("Baki awal berjaya disimpan") → toast.success("Opening balance saved")
    - toast.error("Sila lengkapkan semua medan") → toast.error("Please complete all fields")
    - Any field labels related to "Baki Awal" → "Opening Balance"
    - "Simpan" button → "Save", "Batal" → "Cancel"

    Known replacements for Login.tsx:
    - Any Malay labels for email/password fields, login button, error messages → English equivalents
    - Typical: "Log Masuk" → "Log In", "E-mel" → "Email", "Kata Laluan" → "Password"

    Known replacements for DoctorLanding.tsx:
    - "Permintaan Ubat" or similar → "Drug Request"
    - "Borang Antibiotik" or similar → "Antibiotic Form"
    - Any Malay nav/description text → English

    Known replacements for NoPermission.tsx:
    - Any Malay access-denied message text → English equivalents

    Known replacements for AntibioticFormReadOnly.tsx:
    - All form section labels, field labels, status labels → English

    IMPORTANT: Read each file fully before editing. Do not guess — the actual Malay strings may differ from the known list above. Use the known list as a starting point only.

    DO NOT change: Supabase query strings, database column names, TypeScript property keys mapped to DB columns.
  </action>
  <verify>
    <automated>npx vitest run 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -n "Permintaan Baharu\|Pesakit\|Berjaya\|Simpan\|Batal\|Log Masuk\|Kata Laluan\|Tiada\|berjaya\|gagal" src/components/OpeningBalanceDialog.tsx returns zero matches
    - grep -n "Permintaan Baharu\|Pesakit\|Berjaya\|Simpan\|Batal" src/components/NoPermission.tsx returns zero matches
    - grep -n "Simpan\|Batal\|Diluluskan\|Menunggu\|Ditolak" src/components/AntibioticFormReadOnly.tsx returns zero matches
    - grep -n "Log Masuk\|Kata Laluan\|E-mel\|Berjaya\|Gagal" src/pages/Login.tsx returns zero matches
    - grep -n "Permintaan\|Borang\|Antibiotik\|Ubat" src/pages/DoctorLanding.tsx (review hits — confirm none are display strings, only route paths or component names)
    - npx vitest run exits zero (all tests pass — the 4 scaffold tests from Plan 01 may still fail if not yet translated; acceptable — this task only fixes 5 files not covered by those tests)
  </acceptance_criteria>
  <done>All 5 files contain no Malay user-visible display strings. npx vitest run shows no new test failures (existing failures remain from untranslated pages, new failures not introduced).</done>
</task>

</tasks>

<verification>
After both tasks:
1. `npx vitest run src/components/AppSidebar.test.tsx src/components/DrugFormDialog.test.tsx` exits 0 (GREEN)
2. `npx vitest run` runs without compile errors
3. `grep -r "Permintaan Baharu\|Kawalan Ubat\|Nama Ubat\|Tambah Ubat\|Simpan\|Batal\|Nama ubat" src/components/` returns zero matches
</verification>

<success_criteria>
- AppSidebar.test.tsx passes (ENGL-01 confirmed)
- DrugFormDialog.test.tsx passes (ENGL-02 + ENGL-04 confirmed for form component)
- 7 component/page files are fully translated to English
- Zero Malay user-visible strings remain in the files_modified list
</success_criteria>

<output>
After completion, create `.planning/phases/01-english-ui/01-02-SUMMARY.md` documenting:
- Each file translated and a brief note of what changed
- Test results (AppSidebar.test + DrugFormDialog.test GREEN)
- Any Malay strings encountered that were NOT in the research inventory (edge cases to watch in later plans)
</output>
